before_script:

compile:
  script:
    - ./views/compileviews.sh

test:
  script:
    - ls -l
    - cd views
    - ls -l
    - npm install -g purescript
    - npm install -g pulp bower
    - npm i
    - bower i
    - npm test
