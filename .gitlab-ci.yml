image: node:8

before_script:

compile:
  script:
    - ./views/compileviews.sh

test:
  script:
    - git rebase
    - cd views/tests
    - npm install -g purescript
    - npm install -g pulp bower
    - npm i
    - bower i
    - npm test
